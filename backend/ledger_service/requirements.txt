# Runtime dependencies. Pinned so CI and production resolve identically, and
# pinned to the same versions as the other three services so one Postgres
# driver and one JWT library are in play across the backend.
fastapi==0.141.1
uvicorn[standard]==0.52.4
pydantic==2.13.5
pydantic-settings==2.15.0

# Persistence
SQLAlchemy[asyncio]==2.0.52
asyncpg==0.31.0

# Token verification only. This service never signs.
PyJWT==2.14.0

# [F-7] #96, D-008. The ledger's first outbound HTTP call: service/market_terms.py
# reads a published market's terms from market_service.
httpx==0.28.1

# [F-9] #112. The price broadcast's bus. Pinned to the exact version
# realtime_service carries, so one Redis library is in play across the
# backend, the same rule this file already states for the JWT and Postgres
# libraries.
redis==8.1.0
